{
  "name": "elemwave_website",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --noEmit",
    "watch": "tsc -w --noEmit",
    "test": "jest",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy elemwave-website-${ENVIRONMENT:-staging}"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.13.6",
    "aws-cdk": "2.1143.0",
    "jest": "^30.5.2",
    "ts-jest": "^29.4.13",
    "ts-node": "^10.9.2",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.270.0",
    "constructs": "^10.8.1"
  }
}
