<!--
Base this on `staging`, not `main`: GitHub defaults a new pull request to the
repository default branch, and `main` is the released site.

Delete any heading below that does not apply to this change.
-->

## What this does

## Why

<!-- The problem, not the solution. -->

## How it was verified

<!--
Name which gate ran and what it ran over — this change, or the whole project —
plus whatever a gate cannot show: a browser check, a manual flow, a measurement.
"CI is green" is not verification on its own for anything user-visible.
-->

## Notes for the reviewer
